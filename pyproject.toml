[project]
name = "nycparking"
version = "0.1.0"
description = "NYC parking analytics SQLite workflow"
requires-python = ">=3.11"
dependencies = [
    "pandas",
    "python-dotenv",
    "python-geosupport",
    "requests"
]

[tool.pyright]
extraPaths = ["src"]
